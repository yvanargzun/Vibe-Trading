# Synaptika Chat — Open WebUI + OmniRoute (stability-focused)
# Joins synaptika-trade network; HTTPS via trade Caddy
services:
  omniroute:
    image: diegosouzapw/omniroute:latest
    container_name: synaptika-chat-omniroute
    restart: unless-stopped
    stop_grace_period: 40s
    environment:
      - PORT=20128
      - HOSTNAME=0.0.0.0
      - DATA_DIR=/app/data
      - OMNIROUTE_MEMORY_MB=${OMNIROUTE_MEMORY_MB:-320}
      - NODE_OPTIONS=--max-old-space-size=${OMNIROUTE_MEMORY_MB:-320}
      - INITIAL_PASSWORD=${OMNIROUTE_INITIAL_PASSWORD:-changeme-omniroute}
      - OMNIROUTE_WS_BRIDGE_SECRET=${OMNIROUTE_WS_BRIDGE_SECRET:-}
      - API_KEY_SECRET=${OMNIROUTE_API_KEY_SECRET:-}
      - JWT_SECRET=${OMNIROUTE_JWT_SECRET:-}
      - STORAGE_ENCRYPTION_KEY=${OMNIROUTE_STORAGE_ENCRYPTION_KEY:-}
      - REQUIRE_API_KEY=${OMNIROUTE_REQUIRE_API_KEY:-true}
      # Passthrough client key for /v1/* (Render demos/Messenger, OWUI, Hermes)
      - OMNIROUTE_API_KEY=${OMNIROUTE_GATEWAY_API_KEY:-}
      - ROUTER_API_KEY=${OMNIROUTE_GATEWAY_API_KEY:-}
      - AUTH_COOKIE_SECURE=true
      - NEXT_PUBLIC_BASE_URL=${OMNIROUTE_PUBLIC_URL:-https://synaptika-omni.duckdns.org}
      - BASE_URL=http://omniroute:20128
      - REDIS_URL=redis://omniroute-redis:6379
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-${OPENAI_API_KEY:-}}
      - GOOGLE_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      # Prefer emitting answer text over long reasoning stalls
      - OMNIROUTE_AUTO_FREE_FALLBACK_TO_FULL_POOL=true
    volumes:
      - omniroute_data:/app/data
    ports:
      - "127.0.0.1:20128:20128"
    depends_on:
      - omniroute-redis
    networks:
      - synaptika
    mem_limit: 720m

  omniroute-redis:
    image: redis:7-alpine
    container_name: synaptika-chat-omniroute-redis
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "no", "--maxmemory", "32mb", "--maxmemory-policy", "allkeys-lru"]
    networks:
      - synaptika
    mem_limit: 48m

  # Kept as offline emergency fallback (not wired into OWUI by default)
  chat-llm-proxy:
    build: ./llm_proxy
    container_name: synaptika-chat-llm-proxy
    restart: unless-stopped
    profiles: ["fallback"]
    environment:
      - LLM_PROXY_MODEL=${LLM_PROXY_MODEL:-synaptika-chat-auto}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENROUTER_BASE_URL=${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}
      - OPENAI_API_BASE_URL=${OPENAI_API_BASE_URL:-https://openrouter.ai/api/v1}
      - OPENROUTER_FAILOVER_MODELS=${OPENROUTER_FAILOVER_MODELS:-openrouter/free}
      - ALLOW_PAID_OPENROUTER=${ALLOW_PAID_OPENROUTER:-0}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-flash-latest}
      - GEMINI_BASE_URL=${GEMINI_BASE_URL:-https://generativelanguage.googleapis.com/v1beta/openai}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_MODEL=${GROQ_MODEL:-llama-3.3-70b-versatile}
      - GROQ_BASE_URL=${GROQ_BASE_URL:-https://api.groq.com/openai/v1}
      - LLM_PROXY_TIMEOUT=${LLM_PROXY_TIMEOUT:-60}
    ports:
      - "127.0.0.1:4001:4000"
    networks:
      - synaptika

  chat-webui:
    # Pin version — :main was causing churn on recreate
    image: ghcr.io/open-webui/open-webui:v0.11.0
    container_name: synaptika-chat-webui
    restart: unless-stopped
    environment:
      - WEBUI_NAME=Synaptika Chat
      - WEBUI_URL=https://synaptika-chat.duckdns.org
      - WEBUI_AUTH=True
      - ENABLE_SIGNUP=false
      - ENABLE_LOGIN_FORM=true
      - ENABLE_WEBSOCKET=true
      - DEFAULT_USER_ROLE=admin
      - CORS_ALLOW_ORIGIN=https://synaptika-chat.duckdns.org
      - ENABLE_OLLAMA_API=false
      - ENABLE_EVALUATION_ARENA_MODELS=false
      - ENABLE_IMAGE_GENERATION=false
      - ENABLE_RAG_HYBRID_SEARCH=false
      - ENABLE_PERSISTENT_CONFIG=false
      - ENABLE_WEB_SEARCH=true
      - WEB_SEARCH_ENGINE=duckduckgo
      - WEB_SEARCH_RESULT_COUNT=5
      - WEB_SEARCH_CONCURRENT_REQUESTS=1
      - BYPASS_WEB_SEARCH_EMBEDDING_AND_RETRIEVAL=true
      - BYPASS_WEB_SEARCH_WEB_LOADER=true
      - ENABLE_SEARCH_QUERY_GENERATION=false
      - RAG_EMBEDDING_ENGINE=
      - TASK_MODEL=
      - TASK_MODEL_EXTERNAL=
      # SINGLE upstream only — multi-URL caused 500+ models + UI flicker
      - OPENAI_API_BASE_URL=http://omniroute:20128/v1
      - OPENAI_API_KEY=${OMNIROUTE_GATEWAY_API_KEY:-${OMNIROUTE_OWUI_API_KEY:-}}
      - OPENAI_API_BASE_URLS=http://omniroute:20128/v1
      - OPENAI_API_KEYS=${OMNIROUTE_GATEWAY_API_KEY:-${OMNIROUTE_OWUI_API_KEY:-}}
      - ENABLE_BASE_MODELS_CACHE=true
      - DEFAULT_MODELS=${DEFAULT_MODELS:-auto/chat}
      - DEFAULT_PINNED_MODELS=auto/chat;auto/fast;auto/best-free
      - MODEL_ORDER_LIST=["auto/chat","auto/fast","auto/best-free","auto/coding:free"]
      - DEFAULT_MODEL_PARAMS={"stream_response":true,"temperature":0.4,"max_tokens":2048,"function_calling":"legacy"}
      - DEFAULT_MODEL_METADATA={"capabilities":{"web_search":true,"citations":true,"file_upload":true},"defaultFeatureIds":[]}
      # Longer client timeouts so long streams are not cancelled mid-flight
      - AIOHTTP_CLIENT_TIMEOUT=600
      - AIOHTTP_CLIENT_TIMEOUT_MODEL_LIST=30
      - WEBSOCKET_MANAGER=
      - WEBUI_SECRET_KEY=${WEBUI_SECRET_KEY:-change-me-synaptika-chat}
    volumes:
      - open_webui:/app/backend/data
      - ./SYSTEM_PROMPT.md:/srv/SYSTEM_PROMPT.md:ro
      - ./enable_web_search.py:/srv/enable_web_search.py:ro
      - ./apply_system_prompt.py:/srv/apply_system_prompt.py:ro
      - ./fix_web_search_ui.py:/srv/fix_web_search_ui.py:ro
      - ./fix_web_search_legacy.py:/srv/fix_web_search_legacy.py:ro
      - ./fix_followup_hang.py:/srv/fix_followup_hang.py:ro
      - ./fix_stability.py:/srv/fix_stability.py:ro
    depends_on:
      - omniroute
    networks:
      - synaptika
    mem_limit: 1024m

volumes:
  open_webui:
  omniroute_data:

networks:
  synaptika:
    name: synaptika-trade_default
    external: true
